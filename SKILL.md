---
name: "jjam-1min"
description: "교과서 차시 PDF를 NotebookLM 숏폼(9:16, 약 1분)으로 만들어 검수하고, 유튜브 Shorts로 공개한 뒤 '짬짬이 1분 수업' 사이트에 등록하는 전 과정. 사용자가 '사회 5-2 2단원 3차시 숏폼 만들어줘', '짬짬이 1분 수업에 차시 추가해줘', '차시 영상 만들어서 유튜브에 올려줘' 같은 요청을 하거나, 만들어 둔 mp4를 유튜브에 올리고 사이트에 반영해 달라고 할 때 쓴다. 교과서 학기 PDF를 차시로 자르는 일은 edufine/lesson-kit 쪽이고 이 스킬의 범위가 아니다."
---

# 짬짬이 1분 수업 — 차시 숏폼 만들기

교과서 차시 PDF 한 개 → 1분 세로 영상 → 유튜브 Shorts → 사이트 등록.

**차시마다 영상을 두 편 만들고, 유튜브에는 두 편 다 올린다.**
한 편만 만들면 결함이 있어도 그것을 쓸 수밖에 없다.

**사이트에 올릴 한 편은 사용자가 고른다.** 에이전트는 두 편의 프레임 시트와
유튜브 링크를 나란히 보여 주고 각 편의 결함을 적어 주되, **고르지는 않는다.**
수업에 쓸 사람이 판단할 일이다.

한 차시에 **생성 13분(두 편 동시) + 검수 8분 + 업로드 6분(두 편) + 사이트 2분**이 든다.

밟았던 함정은 `references/troubleshooting.md` 에 모아 두었다.
막히면 거기서 먼저 찾는다 — 대부분 한 번 당해 본 것이다.

## 처음 쓰는 컴퓨터에서

경로는 컴퓨터마다 다르므로 **스킬 폴더의 `config.json`** 에 적는다.
`config.sample.json` 을 복사해 채운다(이 파일은 저장소에 올라가지 않는다).

| 키 | 무엇 |
|---|---|
| `siteDir` | 사이트 소스 — `jjam-classroom` 클론의 `1min` 폴더 |
| `workDir` | mp4·로그를 쌓는 작업 폴더 |
| `textbookDir` | 차시 PDF 가 든 교과서 폴더 |
| `defaults` | 출판사·과목·학년·학기 기본값 |

그리고 이 넷이 갖춰져야 돈다.

1. **브라우저가 구글에 로그인**돼 있을 것 — NotebookLM과 유튜브를 둘 다 그 세션으로 쓴다
2. **차시별로 잘린 교과서 PDF** — 학기 PDF 를 차시로 자르는 일은 이 스킬 밖이다(lesson-kit)
3. **사이트 저장소 클론** — 소스는 `jjam-classroom` 의 `1min` 폴더에 있다
4. **ffmpeg** — `pip install imageio-ffmpeg` 면 충분하다. `frames.mjs` 가 알아서 찾는다

유튜브 채널과 재생목록 이름도 `config.json` 에 적어 둔다. NotebookLM과 유튜브는
**같은 구글 계정**이어야 한다(영상을 만든 계정으로 올린다).

## 한도 — 먼저 확인하고 계획을 세운다

| 병목 | 한도 |
|---|---|
| NotebookLM | **5시간 창에 영상 10~11편** (1편당 약 9.4%) |
| | 두 판씩 만들므로 **차시로는 5개** |
| NotebookLM 주간 | 영상 약 225편 = 차시 약 110개 |
| **유튜브 업로드** | **24시간에 10~15편** (고급 기능 전). 두 편씩 올리므로 **차시로는 5~7개** |

NotebookLM 사용량은 노트북 → 스튜디오 → 동영상 개요 → `AI 사용량`에서 본다.
**생성 직전에는 열지 마라** — 그 창을 닫으면 설정 대화상자까지 같이 닫힌다.
재설정 시각도 거기 나온다(5시간 주기, 예: 11:25 → 16:25 → 21:25).

유튜브 한도는 **달력 날짜가 아니라 직전 24시간**을 센다. 저녁까지 15편을 올리면
다음 날 오전에도 막혀 있고, 올린 시각으로부터 24시간이 지나야 한 자리씩 풀린다.

### 유튜브 한도를 올리는 길은 '고급 기능'이다

전화번호 인증만으로는 안 된다. 그건 **중급 기능**이고, 일일 업로드 수를 늘리는 것은
**3단계 고급 기능**이다. 켜져 있는지 먼저 확인한다.

```
studio.youtube.com → 설정 → 채널 → 기능 사용 자격 요건
  1. 표준 기능 · 2. 중급 기능 · 3. 고급 기능  ← 이게 '사용 설정됨' 이어야 한다
```

고급 기능 요구사항은 "중급 기능 사용 설정됨" + **다음 중 하나**다.

| 방법 | 걸리는 시간 |
|---|---|
| **영상 인증** (휴대폰으로 얼굴 촬영) | 몇 시간. 가장 빠르다 |
| 유효한 신분증 | 계정에 따라 **선택 자체가 막혀 있을 수 있다** |
| 채널 기록 | 약 30일 |

영상 인증은 QR 을 휴대폰으로 스캔해 진행한다. **본인 확인이라 대신할 수 없다** —
화면만 띄워 주고 사용자가 끝내게 한다. 제출하면 3단계가 `대기 중` 으로 바뀐다.

고급 기능이 없는 동안에는 **업로드를 몰아치지 말고 나눠 올린다.**
Shorts 는 하루 10편을 넘기면 스팸 탐지에 걸려 일시 차단될 수 있다.

**켜진 뒤에는 병목이 NotebookLM 쪽으로 옮겨간다.** 이 저장소를 만든 채널은
2026-09-23 에 승인됐다. 승인 뒤의 유튜브 일일 상한은 **아직 실측하지 않았으니**
그 숫자로 계획을 세우지 말고, **5시간 창에 10~11편**이라는 NotebookLM 쪽 숫자로
세운다. 실제로 몰아서 올려 본 뒤에 이 자리에 실측값을 적는다.

---

## 1단계 · 생성 (브라우저 UI)

CLI(`notebooklm-py`)는 인증이 막혀 있어 쓰지 않는다. 브라우저는 이미 로그인돼 있고
세션 만료 걱정이 없어 오히려 낫다.

REPL `fs`는 세션 폴더 밖을 못 읽으므로 **업로드할 PDF를 먼저 세션 tmp로 복사**한다.
(`textbookDir` 아래의 차시 PDF 를 복사해 쓴다.)

1. `https://notebook.google.com/` → **새로 만들기** → 파일 업로드 → **차시 PDF 하나만**
2. 노트북 이름을 `짬짬이1분_<과목><학년학기>_<단원>단원_<NN>차시_<주제>` 로
3. 소스 처리 완료 대기 (진행바가 사라질 때까지, 보통 10초 이내)
4. 스튜디오 → **동영상 개요**
5. 확인: 형식 `짧은 버전`(9:16, 기본값) · 언어 `한국어`(기본값) · **소스 1개**
6. 집중 주제 칸에 프롬프트 입력 — 골격은 `references/prompt.md`
7. **지금 생성**
8. **같은 프롬프트로 한 번 더 `지금 생성`** — 두 판을 만든다.
   같은 노트북 안에서 두 번 걸면 스튜디오에 항목이 둘 생긴다
9. 약 13분 뒤 둘 다 완료되면 항목마다 `더보기` → **다운로드**

두 판을 같은 프롬프트로 만드는 것이 요점이다. 프롬프트를 바꿔 만들면 무엇 때문에
좋아졌는지 알 수 없다. 같은 조건에서 뽑아 **결과의 편차만 고르는 것**이다.

**한 판이 실패하는 일이 있다.** 그때는 실패본을 지우고 다시 걸되, 한도가 빠듯하면
성공한 한 판으로 간다 — 두 판은 더 나은 것을 고르기 위한 것이지 필수 조건이 아니다.

**차시마다 노트북을 새로 만든다.** 기존 노트북에 원고가 섞여 있으면 소스 선택을
빠뜨렸을 때 엉뚱한 자료로 만들어진다. 소스가 하나뿐이면 그 사고가 안 난다.

### 여러 편을 한꺼번에 돌린다

생성은 서버에서 돌아가므로 **노트북을 떠나도 계속된다.** 그래서 순서대로 기다릴
필요가 없다. 노트북을 만들어 생성만 걸고 다음 차시로 넘어가면 된다.

실측: 7편을 5분 안에 차례로 걸었더니 **16분 만에 7편이 모두 끝났다**(순차로 하면 90분).
한 창의 한도(영상 10~11편 = 차시 5개)까지는 이렇게 몰아서 거는 것이 맞다.

두 판을 만들 때도 한꺼번에 건다. 차시 5개면 노트북 5개를 만들고 각각 두 번 걸어
영상 10편을 띄워 두면, 15분쯤 뒤에 열 편이 다 내려온다.

### 한도가 거의 찼을 때

`지금 생성`이 사라지고 `나중에 생성`만 남으면 한도에 닿은 것이다. 예약은 걸리지만
**정시에 자동 실행되지 않는 것을 확인했다**(재설정 20분 뒤에도 대기 상태였다).
그리고 **예약에도 상한이 있다** — 6건까지 걸리고 7번째는 `나중에 생성`까지 비활성화됐다.

그래서 한도가 거의 찼다고 판단되면 **예약 슬롯을 먼저 채우고** 남은 용량을
`지금 생성`에 쓴다. 반대로 하면 마지막 몇 편이 예약조차 못 걸린다.

한도가 풀리면 예약을 지우고(`더보기` → `삭제`) 직접 `지금 생성`으로 건다.

## 2단계 · 검수

`scripts/frames.mjs`로 프레임 시트를 뽑아 교과서 본문과 눈으로 대조한다.

```bash
node scripts/frames.mjs <mp4경로> <출력.png>
```

- 규격: **720×1280**, 60~95초
- 보는 것: 지정 용어가 바뀌지 않았나 · 교과서에 없는 내용이 들어갔나 ·
  지도와 유물 모양이 교과서와 맞나 · 화면 글자 맞춤법
- **여러 시대를 한 편에 담는 차시(단원 도입·단원 마무리)는 특히 꼼꼼히 본다.**
  시대별 복식과 무장을 헷갈린다 — 청동기 인물에 갑옷과 투구를 입히고,
  통일신라 장면에 조선식 한복을 그린 적이 있다. 프롬프트의 "그 시대에 없던 물건"
  조항으로도 막히지 않았다
- 자막에 한국어가 아닌 글자가 섞이는지 본다(`5s` 같은 조각이 찍힌 적이 있다)
- **이 단계는 자동화가 대신하지 못한다.** NotebookLM은 내용을 바꾼다.
  고유명사를 바꾸고, 없던 문장을 지어내고, 한반도를 육각형으로 그린 적이 있다.

### 두 편을 나란히 보여 준다 — 고르는 것은 사용자다

두 편 모두 프레임 시트를 뽑아 **한 답변 안에 나란히 붙이고**, 각 편의 결함을 적는다.
볼 것은 이 넷이다.

1. **교과서에 없는 내용** — 지어낸 사실, 그 시대에 없던 물건, 틀린 지도
2. **화면 글자** — 깨진 자막, 한국어 아닌 조각, 맞춤법
3. **서양·중국풍 인물과 영어 라벨** — 실제로 가장 자주 나오는 오류다(아래 표)
4. 길이와 화면 밀도 — 유물이 썸네일만 하게 작아 교실에서 안 보이는 편이 있다

에이전트는 **결함을 빠짐없이 적는 것까지만** 한다. 추천을 덧붙이는 것은 괜찮지만
결정은 사용자에게 맡긴다.

### 자주 나오는 오류 — 서양·중국풍과 영어

두 번째 편들에서 같은 종류의 오류가 반복해 나왔다. 프레임 시트에서 먼저 찾는다.

| 실제로 나온 것 | 어디서 |
|---|---|
| 로마 병사(둥근 방패·로마식 갑옷) | 04차시 · 19차시(원성왕릉 무인상) |
| 서양식 왕관을 쓴 백인 노인 왕 | 06차시 |
| 중국풍 거대 성벽 | 06차시 |
| 영어 라벨 (`Copper ore` `Stone crucible` 등) | 04차시 다이어그램 |

프롬프트에 "영어를 장식으로도 넣지 마세요"가 있는데도 뚫렸다.
**"인물은 모두 한국 사람으로 그리고 서양·중국 복장을 입히지 마세요"** 조항을 같이 넣는다.

**버린 편이 아니라 고를 수 있는 편이다.** 한쪽이 어떤 결함은 고치면서 다른 결함을
만드는 일이 잦으므로 둘 다 남기고 둘 다 올린다.

### 이름 규칙

두 편은 만든 순서대로 1, 2 다. **1번이 더 낫다는 뜻이 아니다** — 사용자가 고를 때까지
둘은 대등하다.

```
<workDir>/output/<과목><학년학기>_<단원>단원_<NN>차시_<주제>/
  02_숏폼1.mp4        먼저 만든 편
  02_숏폼2.mp4        나중에 만든 편
  99_검수.md          각 편의 결함, 유튜브 링크, 사용자가 고른 편
```

한 편뿐이면 번호를 붙이지 않는다(`02_숏폼.mp4`).
무엇이 왜 틀렸는지는 파일 이름이 아니라 `99_검수.md` 에 적는다.
같은 결함이 여러 차시에서 반복되면 그때 프롬프트 조항을 하나 더한다.

### 둘 다 못 쓸 때

두 편 모두 결함이 크면 **다시 만들지 그냥 쓸지 사용자에게 묻는다.**
재생성은 13분이고 사용량을 더 쓰지만, 수업에 쓰기 어려운 영상이면 의미가 없다.

## 3단계 · 유튜브 업로드

studio.youtube.com → 동영상 업로드 → **파일 선택**(대시보드 버튼만으로는 파일 선택창이 안 뜬다).

**두 편 다 올린다.** 제목 끝에 `(1편)` `(2편)` 을 붙여 구분하고,
**둘 다 공개 · 둘 다 재생목록에 넣는다.** 사이트에 어느 편을 걸지는 나중 일이고,
사용자가 유튜브에서 직접 보고 고를 수 있어야 한다.

**올릴 mp4 를 세션 tmp 로 복사한 뒤 준다.** 작업 폴더의 파일을 바로 주면
`Path escapes the session directory` 로 막힌다. NotebookLM 소스 업로드와 같은 제약이다.

| 항목 | 값 |
|---|---|
| 제목 | `초등 <과목> <학년-학기> \| <단원>단원 <N>차시 <주제> \| 짬짬이 1분 수업 (1편)` / `(2편)` |
| 설명 | 차시명 한 줄 + "다루는 내용" 3~4줄 + `#초등사회 #사회5학년2학기 #짬짬이1분수업` |
| 재생목록 | `짬짬이 1분 수업 \| 사회 5-2` |
| 카테고리 | **교육** |
| 동영상 언어 | **한국어** |
| 시청자층 | **아니요, 아동용이 아닙니다** |
| AI 사용 | **예** |
| 공개 상태 | **공개** → 게시 |

**아동용으로 잡으면 안 된다** — 재생목록·알림 등이 막힌다. 실제 시청자는 교사다.
**AI 사용은 반드시 '예'** — AI 합성 나레이션은 유튜브 정책상 공개 대상이고,
게시된 영상에 `AI` 라벨이 붙는다.

업로드 직후에는 영상 링크가 아직 안 뜰 수 있다. **videoId 를 못 읽었으면 실패로
보지 말고**, 게시를 마친 뒤 `채널 콘텐츠 → Shorts` 목록에서 확인한다.

## 4단계 · 사이트 등록

**사용자가 고른 한 편만 등록한다.** 고르기 전에는 등록하지 않는다 —
두 편의 프레임 시트와 유튜브 링크를 보여 주고 답을 기다린다.

`scripts/add-lesson.mjs`로 데이터를 더한 뒤 검증하고 푸시한다.

```bash
node scripts/add-lesson.mjs --lesson 13 --topic "고려의 건국" \
  --title "고려는 어떻게 세워졌을까요" --yt 8jVUv4Uerfk --sec 88 \
  --summary "신라 말의 혼란과 호족의 성장, ..." \
  --keywords "호족,견훤,후백제,궁예,왕건,개경"
```

출판사·과목·학년·학기는 `config.json` 의 `defaults` 를 따른다. 다르면
`--publisher --subject --grade --semester --unit --unit-title` 로 준다.
**단원의 첫 차시**를 넣을 때는 `--unit-title` 이 필요하다.

그다음 사이트 폴더에서 검증 네 개를 모두 돌린다. 하나라도 실패하면 CI도 실패한다.

```bash
node scripts/validate-data.mjs        # 필수 필드·중복·단원 제목 일관성
node scripts/gen-data.mjs             # 파생 파일 재생성 (CI가 --check 로 강제)
node scripts/check-font-coverage.mjs  # 서브셋 폰트에 없는 글자
node scripts/check-sources.mjs        # 내 채널의 살아 있는 영상인지
```

커밋 후 `git push`. 소스 저장소(`jjam-classroom`)와 발행 저장소(`jjam-1min`)가 나뉘어 있어
머지되면 발행 신호가 간다. 발행 저장소 쪽 토큰 권한이 없으면 신호가 HTTP 403 으로 막히므로,
그때는 **발행 저장소 Actions → `발행` → Run workflow** 를 손으로 돌린다.
(안 돌려도 그쪽 하루 한 번 크론이 안전망이라 늦어도 다음 날 아침에는 반영된다.)

반영 확인: 공개 주소의 `data/lessons.index.json` 건수.

---

## 알아 둘 것

**차시 번호는 이어지지 않는다.** 진도표에 따라 결번이 있다 — 사회 5-2 1단원은
11·15·18차시가 없어 17차시뿐이다. 번호가 연속이라고 가정하고 세면 틀린다.
언제나 `textbookDir` 의 실제 파일 목록을 기준으로 삼는다.

**사이트를 로컬에서 확인할 때**는 `python -m http.server` 를 띄운다. 다만 이 서버는
셸이 끝나면 함께 죽으므로, 살려 두려면 독립 프로세스로 만든다.

```powershell
([wmiclass]'Win32_Process').Create("cmd.exe /c cd /d `"$siteDir`" && python -m http.server 8123")
```

**확인이 끝나면 반드시 종료한다** — 검은 창이 쌓여 사용자를 불편하게 한다.

```powershell
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'http\.server' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

고친 것이 화면에 안 보이면 브라우저 HTTP 캐시다(python 서버는 캐시 헤더를 안 준다).
`fetch(url, {cache:'reload'})` 로 확인하거나 **다른 포트로 새로 띄운다.**
실서비스에서는 발행 워크플로가 서비스워커 캐시 이름을 커밋 해시로 바꿔서 이 문제가 없다.
